[workspace]
members = ["."]
resolver = "3"

[package]
name = "epic-platform"
version = "0.1.0"
edition = "2024"
rust-version = "1.94"
description = "A local-first personal investment platform"

[lib]
crate-type = ["cdylib", "rlib"]

[[bin]]
name = "epic-platform"
path = "src/main.rs"
required-features = ["ssr"]

[dependencies]
leptos = "0.8.20"
leptos_meta = "0.8"
leptos_router = "0.8"
axum = { version = "0.8.9", optional = true }
leptos_axum = { version = "0.8", optional = true }
tokio = { version = "1", features = ["macros", "rt-multi-thread", "net", "signal"], optional = true }
# Keep the CLI version in README in sync with this exact version.
wasm-bindgen = { version = "=0.2.128", optional = true }
console_error_panic_hook = { version = "0.1", optional = true }

[features]
default = []
ssr = ["dep:axum", "dep:leptos_axum", "dep:tokio", "leptos/ssr", "leptos_meta/ssr", "leptos_router/ssr"]
hydrate = ["leptos/hydrate", "dep:wasm-bindgen", "dep:console_error_panic_hook"]

[profile.wasm-release]
inherits = "release"
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"

[package.metadata.leptos]
output-name = "epic-platform"
site-root = "target/site"
site-pkg-dir = "pkg"
style-file = "style/main.css"
site-addr = "127.0.0.1:3000"
reload-port = 3001
bin-features = ["ssr"]
bin-default-features = false
bin-cargo-args = ["--locked"]
lib-features = ["hydrate"]
lib-default-features = false
lib-cargo-args = ["--locked"]
lib-profile-release = "wasm-release"
